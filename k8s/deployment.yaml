apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  replicas: 1 # 👈 Mudamos para 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: soat12acr.azurecr.io/oficina-order:latest 
        ports:
        - containerPort: 3000
        # 👇 Apagamos o bloco 'resources' daqui
        env:
        - name: NODE_ENV
          value: "production"
        - name: DATABASE_URL
          value: "postgresql://postgres:vD3WTW0gGmahJdjCfXHG@soat12-postgres-fsmt.postgres.database.azure.com:5432/order_db"
        - name: RABBITMQ_URL
          value: "amqp://rabbitmq-service:5672"
